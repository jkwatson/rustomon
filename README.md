# Shadowdark monster finder

## Prerequisites
Rust and Cargo are required to build and run this project.

### build:
`cargo build --release`
The executable will then be available in the `target/release` directory.


### run:
`cargo run --release`

